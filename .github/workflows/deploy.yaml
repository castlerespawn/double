name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mevanmanilka/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mevanmanilka/react-test npm test

      - run: docker build -t mevanmanilka/multi-client ./client
      - run: docker build -t mevanmanilka/multi-nginx ./nginx
      - run: docker build -t mevanmanilka/multi-server ./server
      - run: docker build -t mevanmanilka/multi-worker ./worker

      - run: docker push mevanmanilka/multi-client
      - run: docker push mevanmanilka/multi-nginx
      - run: docker push mevanmanilka/multi-server
      - run: docker push mevanmanilka/multi-worker
